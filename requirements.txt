# Core runtime dependencies (pinned versions as tested)
PySide6>=6.6
platformdirs>=4.0
Pillow>=10.0
pillow-heif>=0.13
rawpy>=0.19
numpy>=1.26
onnxruntime>=1.17
ImageHash>=4.3
PyWavelets>=1.4
reverse_geocoder>=1.5

# Development / testing
pytest>=7.0

# Optional ML models (heavy — install separately if needed)
# insightface>=0.7          # face detection + recognition
# open-clip-torch>=2.20     # CLIP topic tagging
